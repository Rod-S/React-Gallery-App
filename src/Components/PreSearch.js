import React from 'react';

//initial message component on app load
const PreSearch = props => (

  <li className="not-found">
    <h3>Begin Your Search</h3>
    <p>Click a Photo Category or Search for a Custom Category.</p>
  </li>
);

export default PreSearch;
